platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/oci-build-task

params:
  CONTEXT: bridge-devices-pos

inputs:
  - name: bridge-devices-pos

outputs:
  - name: image

run:
  path: build
